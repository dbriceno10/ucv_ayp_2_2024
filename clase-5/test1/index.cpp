#include <iostream>
#include <cmath>
using namespace std;

int main()
{
  // cout << 5522 / 10 << endl;
  // cout << 5522 % 10 << endl;
  // cout << pow(2,0) << endl;
  const int length = 5;
  int array[length] = {1, 2, 3, 4, 5};
  for (int i = 0; i < length; i++)
  {
    cout << array[i] << endl;
  }
  return 0;
}