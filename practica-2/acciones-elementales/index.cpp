#include <iostream>
#include <cmath>
using namespace std;
int main()
{
  /* code */
  // int i = 1, j = 2;
  // float r = 2.0;
  // bool b = true;
  int a = 3, b = 4;
  bool c = true;

  // #1
  // a)
  // i = (i + 2) * r; //---> 6
  // cout << "El valor de i es " << i << endl;

  // b)
  // b = i / (2 + j) <= 5 * j / 3; //---> 1 o true
  // cout << i / (2 + j) << " <= " << 5 * j / 3 << endl;
  // cout << "El valor de b es " << b << endl;

  // c)
  // i = 11 / 2; //---> 5
  // cout << "El valor de i es " << i << endl;

  // d)
  // r = 11 / (float)2; //---> 5.5
  // cout << "El valor de r es " << r << endl;

  // e)
  // b = b && i + j >= j * 5 % 3; //---> 1 o true
  // cout << (b && i + j) << " >= " << (j * 5 % 3) << endl;
  // cout << "El valor de b es " << b << endl;

  float r = pow(b/2-4*a*b,1/2);
cout <<pow(-4, 0.5)<< endl;

  return 0;
}
